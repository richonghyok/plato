[project]
name = "fedrolex"
version = "0.1.0"
description = "Model-Heterogeneous Federated Learning with Rolling Sub-Model Extraction"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "einops",
    "ptflops"
]
